#Ernesto Cortez       #Josue Urzua     #Herminio Beltran
#Chris Luna           #Jesus Ruiz  

from theater import*
from weekdays import*
from pas import*


age = int(input("What is your age? "))
theater(age)

day = input("What day of the week is it? ")
weekdays(day)

password = input('Give a password: ')
pas(password)

